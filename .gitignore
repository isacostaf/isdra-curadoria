node_modules/
.env
.env.local
*.log
.DS_Store
.vercel

# projeto de referência (outro projeto, só usado pra copiar o fundo dinâmico
# em public/js/dynamic-background.js) — não faz parte deste app
PraticPilates/

# arquivos locais legados (o app agora guarda tudo no Supabase) —
# mantidos só para quem ainda precisa rodar a migração local -> nuvem
data/db.json
data/MIGRATION_CREDENTIALS.txt
uploads/project/*
uploads/folders/*
uploads/items/*
!uploads/project/.gitkeep
!uploads/folders/.gitkeep
!uploads/items/.gitkeep
